const revealObserver=new IntersectionObserver(e=>{e.forEach(t=>{t.isIntersecting&&t.target.classList.add("is-visible")})},{threshold:.18});document.querySelectorAll(".reveal").forEach(e=>revealObserver.observe(e));const steps=document.querySelectorAll(".step"),demoCard=document.querySelector(".demo-card");steps.forEach(e=>{e.addEventListener("click",()=>{steps.forEach(e=>e.classList.remove("active"));e.classList.add("active");demoCard.dataset.step=e.dataset.step})});let activeStep=0;setInterval(()=>{activeStep=(activeStep+1)%steps.length;steps[activeStep].click()},3600);const comparison=document.querySelector("[data-comparison]"),cleanSide=document.querySelector("[data-clean-side]"),compareLine=document.querySelector("[data-compare-line]"),compareRange=comparison.querySelector(".compare-range");function setComparison(e){cleanSide.style.clipPath=`inset(0 0 0 ${e}%)`;compareLine.style.left=`${e}%`}compareRange.addEventListener("input",e=>setComparison(e.target.value));setComparison(compareRange.value);document.querySelector("[data-signup]").addEventListener("submit",e=>{e.preventDefault();const t=e.currentTarget,o=t.querySelector(".form-status"),n=t.email.value.trim();if(!n){o.textContent="Enter your email to join the waitlist.";return}o.textContent="You're on the tiffytab waitlist. We'll keep you posted.";t.reset()});